---
# yaml-language-server: $schema=../schemas/host.schema.json

# General
docker_enabled: false
tailscale_enabled: false
packages_install_shell: false
packages_install_network: false

# Dotfiles
dotfiles_enabled: false
dotfiles_deploy_to_root: false

# Users
users_list:
  - name: pggm95
    uid: 1000
    gid: 1000
    group: pggm95
    shell: /usr/sbin/nologin
    home: /nonexistent
    create_home: false

# Samba
samba_enabled: true
samba_existing_user: pggm95
samba_password: "{{ lookup('env', 'UNIVERSAL_PASS') }}"
smb_conf_file: ../files/smb.conf
