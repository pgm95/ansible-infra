---
# ============================================================================
# Packages Role - User-Configurable Variables
# ============================================================================
# Variable naming convention:
#   __var      = Internal (computed/consumed within single task file)
#   _var       = Interface (passed between tasks/templates/handlers)
#   role_var   = Role output (computed, used across task files)
#
# System package installation with feature flags for modular deployments.
# All variables are user-configurable via group_vars or host_vars.
#
# Purpose: Install base packages and optional feature sets

packages_fullupgrade: false
packages_install_base: false
packages_install_shell: false
packages_install_network: false
packages_extra_list: [] # (list) Additional packages defined in host_vars

packages_base_list:
  - apt-transport-https
  - ca-certificates
  - xz-utils
  - gnupg
  - rsync
  - curl
  - wget
  - bash
  - git

packages_shell_list:
  - bash-completion
  - ripgrep
  - screen
  - sudo
  - less
  - tree
  - gawk
  - nano
  - bat
  - fzf
  - lsd
  - jq

packages_network_list:
  - bind9-dnsutils
  - traceroute
  - net-tools
  - ethtool
  - whois
  - nmap
